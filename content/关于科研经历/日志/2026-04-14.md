---
date: 2026-04-14 23:33:40
type: fMRI科研日志
tags:
  - fmri预处理
---
## 📌 今日核心任务

- [ ] *ses-016* 预处理的过程中出现了剥头骨过多的问题，观察到受试者的头顶和后脑勺文件缺失，尝试解决。

---

## 💻 命令行/代码记录

### 1. 执行的命令或代码

关于前置部分检查预处理效果代码，请访问[预处理评分](/关于科研经历/fmri/关于CAPs分析包/预处理评分任务)

~~~ 命令反馈
# 关于如何评估预处理结果，请见预处理评分部分代码。
~~~

~~~ 命令反馈
# 这部分代码请访问 /home/data/fenghan/fmri_prep/3_registration.shAD3，这一步用于配准。

### align_epi_anat.py   -epi example_func.nii.gz -epi_base example_func.nii.gz -anat highres.nii.gz -epi2anat -anat_has_skull no -cost lpc+ZZ -giant_move  -align_centers yes -epi_strip 3dAutomask -Allineate_opts "-weight_frac 1.0 -maxrot 10 -maxshf 20 -VERB -warp shr" -master_epi highres.nii.gz

align_epi_anat.py   -epi example_func.nii.gz -epi_base mean -anat highres.nii.gz -epi2anat -anat_has_skull no -cost lpc+ZZ -giant_move  -align_centers yes -epi_strip 3dAutomask -Allineate_opts "-maxrot 45 -maxshf 60 -warp shr" -volreg off -tshift off 
~~~
可以观察到，我的初版代码使用了第二行参数，并观察到了有问题的结果。
更换代码为如下部分：
~~~ 命令反馈
# 这部分代码请访问 /home/data/fenghan/fmri_prep/3_registration_version2.shAD3，这一步用于配准。

align_epi_anat.py   -epi example_func.nii.gz -epi_base example_func.nii.gz -anat highres.nii.gz -epi2anat -anat_has_skull no -cost lpc+ZZ -giant_move  -align_centers yes -epi_strip 3dAutomask -Allineate_opts "-weight_frac 1.0 -maxrot 10 -maxshf 20 -VERB -warp shr" -master_epi highres.nii.gz

### align_epi_anat.py   -epi example_func.nii.gz -epi_base mean -anat highres.nii.gz -epi2anat -anat_has_skull no -cost lpc+ZZ -giant_move  -align_centers yes -epi_strip 3dAutomask -Allineate_opts "-maxrot 45 -maxshf 60 -warp shr" -volreg off -tshift off 
~~~

---

### 2. 处理的文件/数据
- 输入文件：
	使用MyConnectome数据组中的ses-016。
	/home/hanlabundergrad2/Downloads/MyConnectome/preprocessed/fenghan_prep/ses-016/
	以及 xx/fenghan_prep/test1/ses-016。前者使用参数1，后者使用参数2。

- 输出文件：见上。
- 文件位置：见上。

---

## 🤔 今天的困惑

> 把你完全看不懂的地方写下来，哪怕是"为什么用这个参数"这种基础问题

1.  这两个参数有什么区别吗？

---

## ✅ 实际完成

- 观测到更换参数并没有显著改善预处理质量，应当更换预处理pipeline，提高regi质量，减少丢失例如 fmriprep 或者 dpabi。


---

## 🔗 与其他日志的联系

关于本问题的后续，请访问[后续](关于科研经历/日志/2026-04-16)。

---

## 📅 下一步计划

**下次要做的**：
1. 尝试安装 fmriprep 或者 dpabi，具体我们应该查找资源站存储的教程。[资源站处存储了一个关于预处理的教程书网站。](关于科研经历/资源站)

---

## 📎 随手记（草稿区）

-  一条蛆！！！

---

*日志生成于 2026-04-14 23:33:40*