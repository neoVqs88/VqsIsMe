---
date: 2026-04-15 22:16:38
type: fMRI科研日志
tags:
  - 阿尔兹海默症
  - CAPs分析
---
## 📌 今日核心任务

- [ ] rest_res_mni.nii.gz 
- [ ] 对 AD 患者的 CAPs 分析，导出余弦相似度时出现了数据丢失的问题，尝试解决。

---

## 💻 命令行/代码记录

### 1. 执行的命令或代码

代码的运行目录位于 `/home/hanlabundergrad2/CAPsTest/ADTest1/code/` 处。
`slurm_caps_all.sh` 为计算 CAPs 的 slurm 总控脚本。
`caps_analyse_multi.py` 为使用 `neruocaps` 计算包的通用总控脚本。
该总控脚本的 test 版本与其的区别是处理数量的不同，其余均相同。
`SilhouetteCoefficentAnalyze.py` 添加了一些了 总控脚本使用的函数。
`batch_stability_analysis.py` 脚本用于批量计算稳定度。
使用时，请在保证文件已被整理为标准 BIDS 结构之后，运行 `slurm_caps_all.sh`。

运行结果放置在：`/home/hanlabundergrad2/CAPsTest/ADTest1/data/results_1/`
访问结果`/home/hanlabundergrad2/CAPsTest/ADTest1/data/results_1/stability_results/` 可以获得稳定度和余弦相似度。

结果之一如下：（/k6/run19/CAP-4_cosine.csv）

| Region      | High_Amplitude | Low_Amplitude |
| ----------- | -------------- | ------------- |
| Subcortical | 0.1767767      | 0.10646201    |
| Cerebellum  | 0              | 0.14200437    |
| Cont        | 0              | 0.3351554     |
| Default     | 0              | 0.40664428    |
| Limbic      | 0              | 0.15225486    |
| DorsAttn    | 0              | 0.393743      |
| SalVentAttn | 0              | 0.32268405    |
| SomMot      | 0              | 0.44617495    |
| Thalamus    | 0              | 0.12571074    |
| Vis         | 0              | 0.39702135    |
可以观察到大片大片显示为0的数据，不确定这是因为`neurocaps`内部的数学实现还是因为其他原因。
### 处理方法①：
通过观察 `bids_convert.sh`，即转化 AD 原始数据为 BIDS 格式的脚本文件，发现了以下部分代码：

~~~ 命令反馈
# 原始代码
    # 复制预处理数据
    [ -f "anat/anat_brain.nii.gz" ] && cp "anat/anat_brain.nii.gz" "${TARGET_DIR}/derivatives/${PIPELINE_NAME}/${bids_id}/ses-1/anat/${bids_id}_ses-1_desc-brain_T1w.nii.gz"
    [ -f "func/rest_pp_mni.nii.gz" ] && cp "func/rest_pp_mni.nii.gz" "${TARGET_DIR}/derivatives/${PIPELINE_NAME}/${bids_id}/ses-1/func/${bids_id}_ses-1_task-rest_space-MNI152NLin2009cAsym_desc-preproc_bold.nii.gz"
    [ -f "func/rest_pp_mask_mni.nii.gz" ] && cp "func/rest_pp_mask_mni.nii.gz" "${TARGET_DIR}/derivatives/${PIPELINE_NAME}/${bids_id}/ses-1/func/${bids_id}_ses-1_task-rest_space-MNI152NLin2009cAsym_desc-brain_mask.nii.gz"
    # 生成完整的 confounds 文件（运动参数 + FD + CSF + WM）
    if [ -f "func/rest_mc.1D" ] && [ -f "func/nuisance/csf.1D" ] && [ -f "func/nuisance/wm.1D" ]; then
        CONFOUND_FILE="${TARGET_DIR}/derivatives/${PIPELINE_NAME}/${bids_id}/ses-1/func/${bids_id}_ses-1_task-rest_desc-confounds_timeseries.tsv"
~~~

意即，我使用输入给 CAPs 使用的数据是  rest_pp_mni.nii.gz 。经过老师指导，这个数据并不是完全预处理之后的数据，老师建议应当更改为 rest_res_mni.nii.gz 进行处理，观察是否可以得到更好的数据。

### 处理方法②：
老师交付给我的 CAPs 分析代码，位置位于 
`/home/hanteng/ADNI1/x1_project_codel/fc_roi_cap_test_119.m`。

该代码的原理是在已经获得的 30 个 CAPs 的基础上，分析获得的全部时间帧，将它们分配到 30 个CAPs 当中去再进行分析。具体机理分析详见 [分析和使用](关于科研经历/fmri/fc_roi_cap_test_119的分析和使用)。

---

### 2. 处理的文件/数据
- 输入文件：237个AD患者的fmri数据。
- 输出文件：CAPs分析结果。
- 文件位置：`/home/hanlabundergrad2/CAPsTest/ADTest1/data/`
- AD 患者原始全部预处理文件路径为：`/data/fenghan/ADNI_sum/`

---

## 🤔 今天的困惑

> 把你完全看不懂的地方写下来，哪怕是"为什么用这个参数"这种基础问题

1. 老师使用的脚本的原理。
2. 
3. 

---

## ✅ 实际完成

- 
- 
- 

---

## 🔗 与其他日志的联系




---

## 📅 下一步计划

**下次要做的**：
1. 
2. 

**长期目标进度**（可选）：
- 理解预处理流程：
- 掌握 GLM 分析：
- 写脚本自动化：

---

## 📎 随手记（草稿区）



---

*日志生成于 2026-04-15 22:16:38*