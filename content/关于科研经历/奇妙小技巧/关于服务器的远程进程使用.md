~~~ bash
pkill -u hanlabundergrad2 Xvnc
pkill -u hanlabundergrad2 tl-xinit
pkill -u hanlabundergrad2 gnome-session
rm -rf /var/opt/thinlinc/session/hanlabundergrad2/
~~~
~~~
#!/bin/bash
#SBATCH -J myconn_reversed
#SBATCH -p defq
#SBATCH -N 1                    # 只用一个节点
#SBATCH --ntasks-per-node=1     # 1个进程
#SBATCH --cpus-per-task=56      # 用满56个核心
#SBATCH -o reversed_myconn.out
#SBATCH -e reversed_myconn.err
# 设置线程数
export OMP_NUM_THREADS=56
export MKL_NUM_THREADS=56
~~~

~~~
# Add Docker's official GPG key:
sudo -E apt update
sudo -E apt install ca-certificates curl
sudo -E install -m 0755 -d /etc/apt/keyrings
sudo -E curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
sudo -E chmod a+r /etc/apt/keyrings/docker.asc

# Add the repository to Apt sources:
sudo -E tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://download.docker.com/linux/ubuntu
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo -E apt update
# sudo 指令无法继承环境变量，可以用 -E 参数
~~~

找到了用于WSL（Linux）的网络代理，网址位于
~~~

~~~